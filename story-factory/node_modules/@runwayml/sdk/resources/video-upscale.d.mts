import { APIResource } from "../core/resource.mjs";
import { RequestOptions } from "../internal/request-options.mjs";
import { APIPromiseWithAwaitableTask } from "../lib/polling.mjs";
export declare class VideoUpscale extends APIResource {
    /**
     * This endpoint will start a new task to upscale a video. Videos will be upscaled
     * by a factor of 4X, capped at a maximum of 4096px along each side.
     *
     * @example
     * ```ts
     * const videoUpscale = await client.videoUpscale.create({
     *   model: 'upscale_v1',
     *   videoUri: 'https://example.com/video.mp4',
     * });
     * ```
     */
    create(body: VideoUpscaleCreateParams, options?: RequestOptions): APIPromiseWithAwaitableTask<VideoUpscaleCreateResponse>;
}
export interface VideoUpscaleCreateResponse {
    /**
     * The ID of the task that was created. Use this to retrieve the task later.
     */
    id: string;
}
export interface VideoUpscaleCreateParams {
    model: 'upscale_v1';
    /**
     * A HTTPS URL.
     */
    videoUri: string;
}
export declare namespace VideoUpscale {
    export { type VideoUpscaleCreateResponse as VideoUpscaleCreateResponse, type VideoUpscaleCreateParams as VideoUpscaleCreateParams, };
}
//# sourceMappingURL=video-upscale.d.mts.map